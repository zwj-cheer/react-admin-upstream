# 升级说明

本目录为每个稳定发布提供一份指南。

每份指南必须包含：

- 源版本与目标版本；
- 破坏性变更；
- 下游必须执行的操作；
- 预期冲突热点；
- 运行时配置迁移；
- 测试与视觉验证清单。

自动化升级夹具会创建一个临时 Git 仓库，为初始模板打标签，在独立分支提交示例下游项目
overlay，为更新后的模板核心打标签，然后将该相邻标签合并到下游。测试证明项目侧文件保持
逐字节不变，而模板核心文件正常推进。

首个发布没有已发布的上一个稳定版本。下一个稳定发布的发布证据必须额外包含独立示例下游的
结果。
